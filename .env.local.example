# Development Environment Variables
# Copy this file to .env.local for local development

# === DEVELOPMENT MODE ===
# When NODE_ENV=development (automatically set by `npm run dev`):
# - Google OAuth authentication is bypassed with a mock user
# - Autumn billing/credits are bypassed with mock credits (999)
# - No need to set up Google OAuth or Autumn credentials for local dev

# === REQUIRED FOR PRODUCTION ===
# These are only needed when deploying to production:

# NextAuth (Google OAuth) - only needed for production
# NEXTAUTH_SECRET=your-32-char-secret-from-openssl-rand-base64-32
# NEXTAUTH_URL=https://your-domain.com
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Autumn (billing/credits) - only needed for production
# AUTUMN_SECRET_KEY=your-autumn-secret-key
# NEXT_PUBLIC_AUTUMN_THUMBNAIL_FEATURE_ID=credits

# === ALWAYS REQUIRED ===
# Gemini API for thumbnail generation (required in both dev and prod)
GEMINI_API_KEY=your-gemini-api-key

# === DEVELOPMENT NOTES ===
# - Run `npm run dev` to start development server
# - Authentication is automatically bypassed in development
# - Mock user: dev@example.com with 999 credits
# - No Google OAuth setup needed for local development
# - No Autumn setup needed for local development
